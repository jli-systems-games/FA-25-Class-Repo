# 动作面板设置指南（3个独立面板版）

## 🎯 功能说明

现在有**3个独立的面板**，每个功能一个：
- 🍖 **喂食面板** (FeedPanel)
- 🎾 **玩耍面板** (PlayPanel)  
- 🛁 **清洁面板** (CleanPanel)

每个面板有**2种状态**：
1. ✅ **首次点击**：播放动画 + 4秒后自动关闭
2. ✅ **10秒内再次点击**：显示警告文字（不播放动画）+ 4秒后自动关闭
3. ✅ **10秒后再点击**：又重新播放动画

**总共6种不同的弹出效果！**

## 📋 需要在Unity中设置的内容

### 1. 创建3个动作面板

在Canvas下创建3个独立的面板：

```
Canvas
├── FeedPanel ← 喂食面板
│   ├── Background（暗色背景）
│   ├── FeedAnimatedObject（带Animator组件）
│   └── FeedWarningText（警告文本）
│
├── PlayPanel ← 玩耍面板
│   ├── Background（暗色背景）
│   ├── PlayAnimatedObject（带Animator组件）
│   └── PlayWarningText（警告文本）
│
└── CleanPanel ← 清洁面板
    ├── Background（暗色背景）
    ├── CleanAnimatedObject（带Animator组件）
    └── CleanWarningText（警告文本）
```

#### 步骤（创建每个面板都重复一次）：

**面板1 - 喂食面板：**
1. **在Canvas下右键** → **UI → Panel**
2. 命名为 `FeedPanel`
3. 设置：
   - **Anchor**: 居中（Center）
   - **Width**: 500
   - **Height**: 400
   - **背景色**: 可以用不同颜色区分（如橙色调 `255, 200, 150, 200`）
4. **初始隐藏**（取消勾选GameObject）

**面板2 - 玩耍面板：**
1. 重复上述步骤，命名为 `PlayPanel`
2. 背景色可以用绿色调 `(150, 255, 200, 200)`
3. **初始隐藏**

**面板3 - 清洁面板：**
1. 重复上述步骤，命名为 `CleanPanel`
2. 背景色可以用蓝色调 `(150, 200, 255, 200)`
3. **初始隐藏**

### 2. 为每个面板添加动画对象

**在FeedPanel下：**
1. **右键** → **Create Empty**，命名为 `FeedAnimatedObject`
2. 添加 **Image** 组件
3. 添加 **Animator** 组件
4. 设置位置居中

**在PlayPanel下：**
1. 创建 `PlayAnimatedObject`
2. 添加 **Image** 和 **Animator**

**在CleanPanel下：**
1. 创建 `CleanAnimatedObject`
2. 添加 **Image** 和 **Animator**

### 3. 为每个面板添加警告文本

**在FeedPanel下：**
1. **右键** → **UI → TextMeshPro - Text**
2. 命名为 `FeedWarningText`
3. 设置：
   - Text: 留空
   - Font Size: 36-48
   - Color: 黄色或红色
   - Alignment: 居中
4. **初始隐藏**（取消勾选GameObject）

**在PlayPanel下：**
1. 创建 `PlayWarningText`
2. 同样设置

**在CleanPanel下：**
1. 创建 `CleanWarningText`
2. 同样设置

### 4. 为每个面板创建动画

#### A. 为FeedAnimatedObject创建动画

1. 选中 `FeedAnimatedObject`
2. 打开 **Window → Animation → Animation**
3. 点击 **Create** 创建动画：`FeedAnimation.anim`
4. 制作喂食相关的动画（食物掉落、小猫吃东西等）

#### B. 为PlayAnimatedObject创建动画

1. 选中 `PlayAnimatedObject`
2. 创建动画：`PlayAnimation.anim`
3. 制作玩耍相关的动画（玩具移动、小猫跳跃等）

#### C. 为CleanAnimatedObject创建动画

1. 选中 `CleanAnimatedObject`
2. 创建动画：`CleanAnimation.anim`
3. 制作清洁相关的动画（水滴、泡泡等）

#### B. 制作动画（示例 - 喂食动画）

在Animation窗口：
1. 添加 **Image → Sprite** 属性
2. 在时间轴上添加关键帧（0秒、0.5秒、1秒、1.5秒...）
3. 为每个关键帧设置不同的sprite（动画帧）
4. 设置动画为 **Loop** 或 **Once**（根据需要）

**如果没有动画帧图片：**
可以简单地做一个缩放动画：
- 0秒：Scale (1, 1, 1)
- 0.5秒：Scale (1.2, 1.2, 1)
- 1秒：Scale (1, 1, 1)

### 5. 连接到UIManager ⭐ 重要！

1. 选中 `UIManager` GameObject
2. 在Inspector中找到新增的字段：

```
喂食面板：
├── Feed Panel ← 拖入FeedPanel
├── Feed Animator ← 拖入FeedAnimatedObject的Animator组件
└── Feed Warning Text ← 拖入FeedWarningText

玩耍面板：
├── Play Panel ← 拖入PlayPanel
├── Play Animator ← 拖入PlayAnimatedObject的Animator组件
└── Play Warning Text ← 拖入PlayWarningText

清洁面板：
├── Clean Panel ← 拖入CleanPanel
├── Clean Animator ← 拖入CleanAnimatedObject的Animator组件
└── Clean Warning Text ← 拖入CleanWarningText

冷却设置：
├── Action Cooldown: 10（秒）← 可调整冷却时间
└── Action Panel Duration: 4（秒）← 可调整显示时长
```

## 🎨 推荐的UI布局

```
Canvas
├── FeedPanel (Panel) ← 橙色调背景
│   ├── Background (Image)
│   ├── FeedAnimatedObject (Empty + Image + Animator)
│   │   └── 显示喂食动画（食物、小猫吃东西）
│   └── FeedWarningText (TMP) ← 初始隐藏
│       └── "Too soon! Let the cat digest first!"
│
├── PlayPanel (Panel) ← 绿色调背景
│   ├── Background (Image)
│   ├── PlayAnimatedObject (Empty + Image + Animator)
│   │   └── 显示玩耍动画（玩具、小猫跳跃）
│   └── PlayWarningText (TMP) ← 初始隐藏
│       └── "Too soon! The cat needs a rest!"
│
└── CleanPanel (Panel) ← 蓝色调背景
    ├── Background (Image)
    ├── CleanAnimatedObject (Empty + Image + Animator)
    │   └── 显示清洁动画（水滴、泡泡）
    └── CleanWarningText (TMP) ← 初始隐藏
        └── "Too soon! The cat is still clean!"
```

**所有3个面板初始都必须隐藏！**

## 🎬 动画命名规范

代码会自动查找这些动画名称：
- `FeedAnimation` - 喂食动画
- `PlayAnimation` - 玩耍动画
- `CleanAnimation` - 清洁动画

**确保Animator Controller中的动画State名称与上面一致！**

## ⚙️ 可调整参数

在UIManager的Inspector中：

### Action Cooldown（冷却时间）
- **默认**: 10秒
- **说明**: 在此时间内再次点击会显示警告文字
- **建议值**: 5-15秒

### Action Panel Duration（面板显示时长）
- **默认**: 4秒
- **说明**: 面板自动关闭的时间
- **建议值**: 3-5秒

## 📝 警告文本内容

代码会根据动作自动显示不同文字：

- **喂食**: "Too soon! Let the cat digest first!"
- **玩耍**: "Too soon! The cat needs a rest!"
- **清洁**: "Too soon! The cat is still clean!"

如果想修改文字，编辑 `UIManager.cs` 中的 `GetCooldownWarningText()` 方法。

## ✅ 测试步骤

### 测试喂食面板：
1. **运行游戏**
2. **选择喂食并确认** → 应该弹出**橙色喂食面板**并播放喂食动画
3. **等待4秒** → 面板自动关闭
4. **立即再次喂食** → 弹出喂食面板显示警告文字（无动画）
5. **等待10秒后再喂食** → 又播放动画了

### 测试玩耍面板：
1. **选择玩耍并确认** → 应该弹出**绿色玩耍面板**并播放玩耍动画
2. **立即再次玩耍** → 弹出玩耍面板显示警告文字
3. 确认动画和警告文字不同于喂食面板

### 测试清洁面板：
1. **选择清洁并确认** → 应该弹出**蓝色清洁面板**并播放清洁动画
2. **立即再次清洁** → 弹出清洁面板显示警告文字
3. 确认是独立的第三个面板

**总共测试6种状态：**
- ✅ 喂食动画
- ✅ 喂食警告
- ✅ 玩耍动画
- ✅ 玩耍警告
- ✅ 清洁动画
- ✅ 清洁警告

## 🎨 美化建议

### 面板样式
- 使用像素风格的边框sprite
- 添加简单的弹出/缩小动画（Panel自己的动画）
- 背景可以使用带纹理的sprite

### 动画内容
- **喂食**: 显示食物掉落或小猫吃东西
- **玩耍**: 显示玩具移动或小猫跳跃
- **清洁**: 显示水滴或泡泡

### 警告文本样式
- 可以添加抖动效果
- 使用醒目的颜色（黄色或红色）
- 添加图标（❌ 或 ⚠️）

## 🐛 常见问题

**Q: 点击后没有弹出面板？**
A: 检查对应的面板（FeedPanel/PlayPanel/CleanPanel）是否已连接到UIManager

**Q: 没有播放动画？**
A: 检查：
1. Animator组件是否连接
2. Animation Clips是否创建
3. Animator Controller是否设置正确

**Q: 警告文字不显示？**
A: 检查WarningText是否连接到UIManager

**Q: 想修改冷却时间？**
A: 在UIManager的Inspector中调整Action Cooldown值

**Q: 想让面板显示更久？**
A: 在UIManager的Inspector中调整Action Panel Duration值

## 💡 扩展建议

1. **不同的面板样式**: 每个面板用独特的背景图片和边框
2. **添加音效**: 每个面板播放不同的音效
3. **添加粒子效果**: 喂食掉落食物、玩耍飞出星星、清洁冒出泡泡
4. **面板弹出动画**: 使用DoTween或Animation让面板弹出更生动
5. **不同的警告文字**: 根据冷却剩余时间显示不同文字
6. **冷却进度条**: 在面板上显示剩余冷却时间
7. **成功/失败反馈**: 根据金钱是否足够显示不同效果
8. **连击系统**: 连续成功执行动作给予奖励

## 🎨 设计建议

### 喂食面板（橙色/红色调）
- 显示食物图片（猫粮、鱼等）
- 动画：食物从上方掉落
- 背景：餐桌或食盆

### 玩耍面板（绿色/黄色调）
- 显示玩具图片（逗猫棒、毛线球）
- 动画：玩具左右摆动、小猫跳跃
- 背景：草地或游乐场

### 清洁面板（蓝色/青色调）
- 显示清洁用品（浴盆、香波）
- 动画：水滴下落、泡泡上升
- 背景：浴室或水池

**现在你有6种完全独立、可自定义的弹出效果！**

---

完成设置后，你的游戏就有了更丰富的视觉反馈！🎮✨

