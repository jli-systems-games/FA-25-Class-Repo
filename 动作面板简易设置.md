# 动作面板简易设置指南

## 🎯 概述

现在使用**Sprite序列切换**代替Animator，只需要拖入几张图片就能实现动画效果！

## 📋 完整设置步骤

### 第1步：创建面板结构

在你的Canvas下创建3个面板：

```
Canvas
├── FeedPanel（喂食面板）
│   ├── AnimImage（Image - 用于播放sprite动画）
│   └── WarningText（TextMeshPro - 显示警告文字）
│
├── PlayPanel（玩耍面板）
│   ├── AnimImage（Image）
│   └── WarningText（TextMeshPro）
│
└── CleanPanel（清洁面板）
    ├── AnimImage（Image）
    └── WarningText（TextMeshPro）
```

### 第2步：创建喂食面板（FeedPanel）

#### A. 创建面板
1. 右键 **Canvas** → **UI → Panel**
2. 命名为 `FeedPanel`
3. 设置大小：比如 **Width: 500, Height: 400**
4. 设置位置：**居中**
5. 设置背景色：比如**橙色调**（用于喂食）

#### B. 添加动画图片
1. 右键 **FeedPanel** → **UI → Image**
2. 命名为 `AnimImage`
3. 设置大小：比如 **Width: 300, Height: 300**
4. 设置位置：**居中**
5. **暂时留空Source Image**（后面在UIManager中通过sprite数组控制）

#### C. 添加警告文本
1. 右键 **FeedPanel** → **UI → TextMeshPro - Text**
2. 命名为 `WarningText`
3. 文本内容：**留空**（代码会自动填充）
4. 设置：
   - 字体大小：**24-32**
   - 对齐方式：**居中**
   - 颜色：**红色或黄色**
5. 设置位置：**居中**

### 第3步：创建玩耍面板（PlayPanel）

**重复第2步**，但改成：
- 面板名：`PlayPanel`
- 背景色：**蓝色调**（用于玩耍）
- 子对象同样命名为 `AnimImage` 和 `WarningText`

### 第4步：创建清洁面板（CleanPanel）

**重复第2步**，但改成：
- 面板名：`CleanPanel`
- 背景色：**绿色调**（用于清洁）
- 子对象同样命名为 `AnimImage` 和 `WarningText`

### 第5步：连接到UIManager

选中 **UIManager** GameObject，在Inspector中：

#### 喂食面板：
```
Feed Panel → 拖入 FeedPanel
Feed Animation Image → 拖入 FeedPanel/AnimImage
Feed Animation Sprites → Size设为你的sprite数量，然后逐个拖入sprite
Feed Warning Text → 拖入 FeedPanel/WarningText
```

#### 玩耍面板：
```
Play Panel → 拖入 PlayPanel
Play Animation Image → 拖入 PlayPanel/AnimImage
Play Animation Sprites → Size设为你的sprite数量，然后逐个拖入sprite
Play Warning Text → 拖入 PlayPanel/WarningText
```

#### 清洁面板：
```
Clean Panel → 拖入 CleanPanel
Clean Animation Image → 拖入 CleanPanel/AnimImage
Clean Animation Sprites → Size设为你的sprite数量，然后逐个拖入sprite
Clean Warning Text → 拖入 CleanPanel/WarningText
```

#### 动画设置：
```
Action Cooldown: 10（冷却时间，秒）
Action Panel Duration: 4（面板显示时长，秒）
Animation Frame Rate: 0.2（每帧间隔，秒）
```

## 🎨 Sprite准备

### 为每个动作准备2-8张sprite：

**喂食动画：**
- `feed_01.png`
- `feed_02.png`
- `feed_03.png`
- `feed_04.png`

**玩耍动画：**
- `play_01.png`
- `play_02.png`
- `play_03.png`
- `play_04.png`

**清洁动画：**
- `clean_01.png`
- `clean_02.png`
- `clean_03.png`
- `clean_04.png`

### Sprite导入设置：
1. 选中所有sprite
2. Inspector中：
   - **Texture Type**: Sprite (2D and UI)
   - **Pixels Per Unit**: 100
   - **Filter Mode**: Point (no filter) ← 像素风格用这个
   - 点击 **Apply**

## 🎮 测试

### 运行游戏：
1. 按 **↑** 或 **↓** 选择功能
2. 按 **空格键**
3. 查看对应面板：
   - **首次点击**：显示sprite动画（循环播放）
   - **10秒内再次点击**：显示警告文字，无动画
4. 4秒后自动关闭

## ⚙️ 动画速度调整

在UIManager中修改 **Animation Frame Rate**：
- `0.1` = 快速（每帧0.1秒）
- `0.2` = 中速（每帧0.2秒）
- `0.3` = 慢速（每帧0.3秒）

## 📸 Inspector截图对照

### UIManager应该显示：

```
UIManager (Script)
├── UI引用
│   ├── Selection Indicators
│   ├── Stats Bars
│   ├── Money Text
│   └── ...
│
├── 喂食面板
│   ├── Feed Panel ← GameObject
│   ├── Feed Animation Image ← Image组件
│   ├── Feed Animation Sprites ← Sprite数组（拖入多个）
│   └── Feed Warning Text ← TextMeshPro
│
├── 玩耍面板
│   ├── Play Panel
│   ├── Play Animation Image
│   ├── Play Animation Sprites ← Sprite数组（拖入多个）
│   └── Play Warning Text
│
├── 清洁面板
│   ├── Clean Panel
│   ├── Clean Animation Image
│   ├── Clean Animation Sprites ← Sprite数组（拖入多个）
│   └── Clean Warning Text
│
├── 冷却设置
│   ├── Action Cooldown: 10
│   └── Action Panel Duration: 4
│
└── 动画设置
    └── Animation Frame Rate: 0.2
```

## 💡 关键点

### ✅ 必须连接：
- Feed/Play/Clean Panel（3个面板）
- Feed/Play/Clean Animation Image（3个Image）
- Feed/Play/Clean Warning Text（3个TextMeshPro）

### ✅ 可选：
- Feed/Play/Clean Animation Sprites（不拖入就不播放动画）

### ⚠️ 注意：
- **Animation Sprites** 是数组，可以拖入2-8张sprite
- 如果不拖入sprite，面板依然会弹出，只是不会有动画
- sprite会循环播放直到面板关闭

## 🎯 快速测试（没有sprite的情况）

如果暂时没有sprite，可以先这样测试：

1. 只连接Panel和WarningText
2. **不连接**Animation Image和Sprites
3. 运行游戏测试面板弹出功能
4. 等准备好sprite后再连接

这样可以先测试逻辑是否正常！

## 🔧 故障排除

### 问题1：面板不弹出
- 检查Feed/Play/Clean Panel是否已连接
- 检查Console有无错误
- 按F2查看调试信息

### 问题2：动画不播放
- 检查Animation Sprites是否已拖入
- 检查Animation Image是否已连接
- 检查sprite是否正确导入

### 问题3：面板一直显示
- 面板会在游戏启动时自动隐藏
- 不需要手动取消勾选

### 问题4：文字不显示
- 检查Warning Text是否已连接
- 检查TextMeshPro的字体和大小

---

**现在只需要拖入sprite序列就能实现动画，不需要配置Animator了！** 🎉

