# 状态条（进度条）使用指南

## 🎯 概述

游戏使用Unity的**Image组件 + Fill Amount**来实现进度条效果。

## 📋 状态条说明

游戏有3个状态条：
1. **饱食度**（Hunger Bar）- 索引0
2. **心情**（Happiness Bar）- 索引1
3. **清洁度**（Hygiene Bar）- 索引2

## 🔧 创建状态条（两种方法）

### 方法1：使用UI → Slider（简单但不灵活）

**不推荐**，因为Slider组件太复杂，我们只需要显示。

---

### 方法2：使用Image + Fill Amount（推荐）⭐⭐⭐

这是最简单、最灵活的方法！

#### 步骤1：创建背景

1. 右键Canvas → **UI → Image**
2. 命名为 `HungerBarBG`（背景）
3. 设置：
   ```
   Width: 300
   Height: 30
   Color: 深灰色 (R:50, G:50, B:50)
   ```

#### 步骤2：创建填充条

1. 右键 `HungerBarBG` → **UI → Image**
2. 命名为 `HungerBarFill`（填充）
3. 设置：
   ```
   Anchors: 左对齐
   ├── Anchor Min: (0, 0)
   └── Anchor Max: (1, 1)
   
   Offsets: 全0
   ├── Left: 0
   ├── Top: 0
   ├── Right: 0
   └── Bottom: 0
   
   Color: 绿色 (R:0, G:255, B:0) - 饱食度
   
   Image Type: Filled  ← 重要！
   ├── Fill Method: Horizontal
   ├── Fill Origin: Left
   └── Fill Amount: 1
   ```

#### 步骤3：添加图标（可选）

1. 右键 `HungerBarBG` → **UI → Image**
2. 命名为 `HungerIcon`
3. 拖入图标sprite（比如食物图标）
4. 设置大小和位置

#### 步骤4：连接到UIManager

1. 选中 **UIManager**
2. 找到 **Stats Bars** 数组
3. 设置 `Size: 3`
4. 拖入3个Fill Image：
   ```
   Element 0 → HungerBarFill
   Element 1 → HappinessBarFill
   Element 2 → HygieneBarFill
   ```

---

## 🎨 完整结构示例

### 推荐的Hierarchy结构：

```
Canvas
├── StatusBars (Empty GameObject - 用于组织)
│   ├── HungerBar
│   │   ├── HungerBarBG (Image - 背景，深灰色)
│   │   │   ├── HungerBarFill (Image - 填充，绿色) ← 拖到UIManager
│   │   │   └── HungerIcon (Image - 图标，可选)
│   │   └── HungerLabel (TextMeshPro - "饱食度")
│   │
│   ├── HappinessBar
│   │   ├── HappinessBarBG (Image - 背景，深灰色)
│   │   │   ├── HappinessBarFill (Image - 填充，黄色) ← 拖到UIManager
│   │   │   └── HappinessIcon (Image - 图标，可选)
│   │   └── HappinessLabel (TextMeshPro - "心情")
│   │
│   └── HygieneBar
│       ├── HygieneBarBG (Image - 背景，深灰色)
│       │   ├── HygieneBarFill (Image - 填充，蓝色) ← 拖到UIManager
│       │   └── HygieneIcon (Image - 图标，可选)
│       └── HygieneLabel (TextMeshPro - "清洁度")
```

---

## ⚙️ 详细设置步骤（以饱食度为例）

### 1. 创建HungerBar容器

```
1. 右键Canvas → Create Empty
2. 命名为 HungerBar
3. 设置位置：比如 (X: -200, Y: 150)
```

### 2. 创建背景HungerBarBG

```
1. 右键HungerBar → UI → Image
2. 命名为 HungerBarBG
3. Rect Transform:
   Width: 300
   Height: 30
4. Image:
   Color: RGB(50, 50, 50) - 深灰色
   Sprite: 留空或使用自定义背景sprite
```

### 3. 创建填充HungerBarFill（关键！）

```
1. 右键HungerBarBG → UI → Image
2. 命名为 HungerBarFill

3. Rect Transform:
   ├── Anchors Preset: 点击左上角方块，按住Alt+Shift，点击 "Stretch Stretch"
   ├── Left: 0
   ├── Right: 0
   ├── Top: 0
   └── Bottom: 0

4. Image:
   ├── Color: RGB(0, 255, 0) - 绿色
   ├── Image Type: Filled  ← 重要！
   ├── Fill Method: Horizontal
   ├── Fill Origin: Left
   └── Fill Amount: 1.0  ← 满的状态
```

### 4. 创建标签（可选）

```
1. 右键HungerBar → UI → TextMeshPro - Text
2. 命名为 HungerLabel
3. Text: "饱食度"
4. Font Size: 20
5. Alignment: 居中
6. Position: 在进度条上方或左侧
```

---

## 🎨 颜色建议

### 饱食度（Hunger）：
- **满：** 绿色 `RGB(0, 255, 0)`
- **中：** 黄色 `RGB(255, 255, 0)`
- **低：** 红色 `RGB(255, 0, 0)`
- **建议：** 使用绿色，代码会自动根据数值显示（你也可以保持固定颜色）

### 心情（Happiness）：
- **建议：** 黄色 `RGB(255, 200, 0)` 或粉色 `RGB(255, 100, 150)`

### 清洁度（Hygiene）：
- **建议：** 蓝色 `RGB(0, 150, 255)` 或青色 `RGB(0, 255, 255)`

---

## 📐 布局建议

### 布局1：垂直排列
```
┌─────────────────────┐
│      猫咪图像        │
│                     │
│ 🍖 [████████▁▁] 80% │ ← 饱食度
│ 😊 [██████▁▁▁▁] 60% │ ← 心情
│ 🛁 [██████████] 100%│ ← 清洁度
│                     │
│   [喂食][玩耍][清洁]  │
└─────────────────────┘
```

### 布局2：横向排列
```
┌─────────────────────┐
│  🍖[████▁]  😊[███▁]  🛁[█████] │
│                                │
│          猫咪图像               │
│                                │
│    [喂食] [玩耍] [清洁]         │
└────────────────────────────────┘
```

### 布局3：侧边栏
```
┌──────┬──────────────┐
│ 🍖   │              │
│[████]│   猫咪图像    │
│      │              │
│ 😊   │              │
│[███] │              │
│      │              │
│ 🛁   │              │
│[████]│              │
├──────┴──────────────┤
│ [喂食][玩耍][清洁]    │
└─────────────────────┘
```

---

## 🔍 关键参数说明

### Image Type = Filled

这是实现进度条的核心！

```
Image Type: Filled
├── Fill Method: 
│   ├── Horizontal（水平填充）← 推荐
│   ├── Vertical（垂直填充）
│   ├── Radial 90（扇形90度）
│   ├── Radial 180（扇形180度）
│   └── Radial 360（圆形填充）
│
├── Fill Origin:
│   ├── Left（从左开始）← 推荐
│   ├── Right（从右开始）
│   ├── Bottom（从下开始）
│   └── Top（从上开始）
│
└── Fill Amount: 0.0 - 1.0
    ├── 0.0 = 空
    ├── 0.5 = 50%
    └── 1.0 = 满
```

### UIManager如何控制

```csharp
// UIManager.cs 中的代码
private void UpdateStatBar(int index, float value)
{
    // value = 0-100
    statsBars[index].fillAmount = value / 100f;
    // 自动转换为 0.0-1.0
}
```

**示例：**
- `value = 100` → `fillAmount = 1.0` → 100%满
- `value = 50` → `fillAmount = 0.5` → 50%半满
- `value = 0` → `fillAmount = 0.0` → 0%空

---

## ✅ 检查清单

### 创建进度条：
- [ ] 创建背景Image（深灰色）
- [ ] 创建填充Image（子对象，彩色）
- [ ] 设置填充Image为 **Filled** 类型
- [ ] 设置 **Fill Method** 为 **Horizontal**
- [ ] 设置 **Fill Origin** 为 **Left**
- [ ] 设置 **Fill Amount** 为 **1**（测试用）
- [ ] 设置填充Image的Anchors为拉伸（Stretch）

### 连接到UIManager：
- [ ] 选中UIManager
- [ ] 找到 **Stats Bars** 数组
- [ ] 设置 `Size = 3`
- [ ] 拖入3个Fill Image到数组
- [ ] 顺序正确：0=饱食度, 1=心情, 2=清洁度

---

## 🎮 测试

### 1. 手动测试填充
1. 选中 `HungerBarFill`
2. 在Inspector中调整 **Fill Amount**
3. 数值：
   - `1.0` → 满条
   - `0.5` → 半满
   - `0.0` → 空条

### 2. 运行游戏测试
1. 运行游戏
2. 状态条应该满的（100%）
3. 按F2查看调试信息确认数值
4. 等待一段时间，状态条应该慢慢减少

---

## 🎨 美化技巧

### 1. 添加边框
```
在HungerBarBG上添加Outline组件：
Component → UI → Effects → Outline
Color: 黑色
Distance: (1, -1)
```

### 2. 添加渐变色
使用Sprite作为Source Image，制作渐变色的进度条图片

### 3. 添加动画
添加Animator组件，制作进度条增加/减少的动画效果

### 4. 添加图标
在进度条旁边放置对应的图标：
- 🍖 食物图标（饱食度）
- 😊 笑脸图标（心情）
- 🛁 清洁图标（清洁度）

### 5. 添加数字显示
```csharp
// 可选：在进度条旁显示具体数值
// 创建TextMeshPro显示 "80/100"
```

---

## ⚠️ 常见问题

### 问题1：进度条不显示
**检查：**
- [ ] Fill Image是否设置为 **Filled** 类型？
- [ ] Fill Amount 是否 > 0？
- [ ] Image的Color是否可见（不是透明）？
- [ ] 是否在背景Image内部？

### 问题2：进度条不变化
**检查：**
- [ ] 是否连接到UIManager的Stats Bars数组？
- [ ] 数组索引是否正确（0, 1, 2）？
- [ ] CatManager是否在运行？

### 问题3：进度条显示不正确
**检查：**
- [ ] Anchors是否设置为拉伸（Stretch）？
- [ ] Left/Right/Top/Bottom是否都是0？
- [ ] Fill Origin是否正确？

### 问题4：进度条颜色不对
**解决：**
- 在Inspector中直接修改Fill Image的Color

---

## 💡 快速创建技巧

### 创建第一个后复制：
1. 创建并设置好 `HungerBar`
2. **复制** HungerBar（Ctrl+D）
3. 重命名为 `HappinessBar`
4. 修改颜色和位置
5. 再复制一个改成 `HygieneBar`
6. 把3个Fill Image拖到UIManager

这样只需要设置一次，后面复制即可！

---

## 🎯 最小设置（30秒）

如果只想快速测试：

1. **创建3个Image**（直接在Canvas下）
2. **命名：** `HungerBar`, `HappinessBar`, `HygieneBar`
3. **每个设置：**
   - Image Type: **Filled**
   - Fill Method: **Horizontal**
   - Fill Amount: **1**
   - 不同颜色（绿、黄、蓝）
4. **拖到UIManager的Stats Bars数组**
5. **完成！**

---

## 📸 Inspector截图对照

### 正确的Fill Image设置：

```
HungerBarFill (Image)
├── Rect Transform
│   ├── Anchors: Stretch, Stretch
│   ├── Left: 0
│   ├── Right: 0
│   ├── Top: 0
│   └── Bottom: 0
│
└── Image
    ├── Source Image: None (或自定义sprite)
    ├── Color: RGB(0, 255, 0) - 绿色
    ├── Material: None
    ├── Raycast Target: ✓
    ├── Image Type: Filled  ← 重要！
    ├── Fill Method: Horizontal
    ├── Fill Origin: Left
    ├── Fill Amount: 1
    └── Clockwise: ✓
```

---

**现在你可以创建漂亮的状态条了！** 🎉

根据你的UI风格选择合适的布局和颜色，进度条会自动根据游戏数据更新！

