# 金币系统改为时间系统 - 更新说明

## 🔄 主要变更

### 系统设计理念
- ❌ **删除：** 金币系统（喂食和清洁不再需要花钱）
- ✅ **新增：** 游戏时长追踪系统
- ✅ **新增：** 年龄（天数）系统

## 📝 文件修改详情

### 1. CatData.cs

#### 删除：
```csharp
public int money = 100;  // 金钱
```

#### 新增：
```csharp
public float playTime = 0f;         // 游戏时长（秒）- 累计总游戏时间
public float survivalTime = 0f;     // 当前生命存活时间（秒）
public int age = 0;                 // 年龄（天数）
```

#### 新方法：
```csharp
// 获取格式化的游戏时长（HH:MM:SS 或 MM:SS）
public string GetFormattedPlayTime()

// 获取年龄（天数，基于survivalTime）
public int GetAgeDays()
```

#### 重置逻辑：
- `playTime` **不会重置** - 累计所有游戏时长
- `survivalTime` **会重置** - 每次新游戏重新计算
- `age` **会重置** - 每次新游戏从0开始

---

### 2. CatManager.cs

#### 删除的金币逻辑：
```csharp
// 喂食不再需要检查金币
if (catData.money >= cost)
{
    catData.money -= cost;  // ❌ 删除
    ...
}

// 清洁不再需要检查金币
if (catData.money >= cost)
{
    catData.money -= cost;  // ❌ 删除
    ...
}

// 删除AddMoney方法
public void AddMoney(int amount)  // ❌ 删除
```

#### 新增的时间逻辑：
```csharp
void Update()
{
    // 同时更新游戏时长和存活时间
    catData.playTime += Time.deltaTime;      // 累计游戏时长
    catData.survivalTime += Time.deltaTime;  // 当前生命存活时间
    
    // 根据存活时间计算年龄（天数）
    catData.age = catData.GetAgeDays();
}
```

#### 喂食和清洁简化：
- ✅ 现在**免费**喂食
- ✅ 现在**免费**清洁
- ✅ 只增加对应属性，无需扣费

---

### 3. UIManager.cs

#### UI字段变更：
```csharp
// 删除
public TextMeshProUGUI moneyText;  // ❌

// 新增
public TextMeshProUGUI playTimeText;  // ✅ 游戏时长显示
public TextMeshProUGUI ageText;       // ✅ 年龄显示（可选）
```

#### UpdateUI方法：
```csharp
// 之前：显示金币
moneyText.text = "$" + data.money.ToString();  // ❌

// 现在：显示时间
playTimeText.text = data.GetFormattedPlayTime();  // ✅
ageText.text = $"{data.GetAgeDays()}天";         // ✅
```

#### 游戏结束显示：
```csharp
// 之前
gameOverText.text = "BYE DAD";  // ❌

// 现在：包含统计信息
gameOverText.text = $"BYE DAD\n\n存活: {days}天\n游戏时长: {playTime}";  // ✅
```

---

## 🎮 游戏逻辑变化

### 之前（金币系统）：
```
1. 初始金币: 100
2. 喂食花费: $5-$15
3. 清洁花费: $5-$12
4. 金币不足无法执行操作
5. 需要通过小游戏赚钱
```

### 现在（时间系统）：
```
1. 无金币限制
2. 喂食: 免费，随时可用（受冷却限制）
3. 清洁: 免费，随时可用（受冷却限制）
4. 玩耍: 免费，随时可用（受冷却限制）
5. 追踪游戏总时长
6. 追踪当前宠物存活天数
```

---

## 📊 时间系统详解

### 1. playTime（游戏总时长）
- **类型：** float（秒）
- **初始值：** 0
- **更新：** 每帧累加 `Time.deltaTime`
- **重置：** **永不重置**，持续累计
- **显示格式：** 
  - `HH:MM:SS`（超过1小时）
  - `MM:SS`（不足1小时）
- **用途：** 统计玩家总游戏时长

### 2. survivalTime（当前生命存活时间）
- **类型：** float（秒）
- **初始值：** 0
- **更新：** 每帧累加 `Time.deltaTime`
- **重置：** 每次新游戏重置为0
- **用途：** 计算当前宠物存活天数

### 3. age（年龄/天数）
- **类型：** int（天）
- **计算：** `survivalTime / 86400秒`
- **初始值：** 0
- **更新：** 每帧根据survivalTime自动计算
- **重置：** 每次新游戏重置为0
- **显示：** `0天`, `1天`, `2天` ...
- **用途：** 显示宠物年龄

---

## 🎯 Unity设置步骤

### 1. 更新UIManager连接

选中UIManager GameObject，在Inspector中：

**删除连接：**
- ❌ Money Text（已移除）

**新增连接：**
- ✅ **Play Time Text** → 拖入显示游戏时长的TextMeshPro
- ✅ **Age Text** → 拖入显示年龄的TextMeshPro（可选）

### 2. 更新UI文本对象

#### 方案A：修改现有Money Text
1. 选中原来的MoneyText对象
2. 重命名为 `PlayTimeText`
3. 调整位置和文本格式
4. 拖到UIManager的Play Time Text字段

#### 方案B：创建新的文本
1. 创建新的TextMeshPro对象：`PlayTimeText`
2. 设置文本：留空（代码会自动更新）
3. 设置格式：居中，合适字体大小
4. 拖到UIManager的Play Time Text字段

#### 方案C：不显示时间
- 不连接Play Time Text和Age Text
- 游戏依然正常运行，只是不显示时间

---

## 💡 显示方案建议

### 推荐布局1：仅显示游戏时长
```
UI布局：
┌─────────────────┐
│  状态条          │
│  [饱食度]        │
│  [心情]          │
│  [清洁度]        │
│                  │
│  时长: 01:23:45  │  ← PlayTimeText
└─────────────────┘
```

### 推荐布局2：显示游戏时长+年龄
```
UI布局：
┌─────────────────┐
│  状态条          │
│  [饱食度]        │
│  [心情]          │
│  [清洁度]        │
│                  │
│  时长: 01:23:45  │  ← PlayTimeText
│  年龄: 3天       │  ← AgeText
└─────────────────┘
```

### 推荐布局3：极简模式
```
UI布局：
┌─────────────────┐
│  状态条          │
│  [饱食度]        │
│  [心情]          │
│  [清洁度]        │
│                  │
│  3天             │  ← 仅显示AgeText
└─────────────────┘
```

---

## 🎨 文本格式示例

### 游戏时长显示：
```
00:15      → 15秒
05:30      → 5分30秒
45:00      → 45分钟
01:23:45   → 1小时23分45秒
12:00:00   → 12小时
```

### 年龄显示：
```
0天        → 刚出生
1天        → 存活1天（86400秒）
10天       → 存活10天
```

---

## ⚠️ 注意事项

### 1. playTime不会重置
- 即使游戏结束，playTime继续累计
- 这是有意设计的，用于统计玩家总游戏时长
- 类似于游戏的"总游戏时长"统计

### 2. survivalTime会重置
- 每次新游戏，survivalTime重置为0
- 用于计算当前宠物的存活天数
- 游戏结束时显示本次存活天数

### 3. 天数计算
- 使用真实时间计算：86400秒 = 1天
- 如果觉得太慢，可以修改公式加速
- 示例：`survivalTime / 3600f` = 1小时算1天

### 4. UI显示可选
- Play Time Text 和 Age Text 都是**可选**的
- 不连接不会报错，只是不显示
- 根据你的UI设计灵活选择

---

## 🔧 自定义调整

### 加速年龄增长

如果觉得1天=86400秒太慢，修改 `CatData.cs`:

```csharp
public int GetAgeDays()
{
    // 默认：86400秒 = 1天（真实时间）
    return Mathf.FloorToInt(survivalTime / 86400f);
    
    // 快速：3600秒 = 1天（1小时算1天）
    // return Mathf.FloorToInt(survivalTime / 3600f);
    
    // 超快：600秒 = 1天（10分钟算1天）
    // return Mathf.FloorToInt(survivalTime / 600f);
}
```

### 修改时间显示格式

修改 `CatData.cs` 中的 `GetFormattedPlayTime()`:

```csharp
// 当前格式：01:23:45 或 23:45
public string GetFormattedPlayTime()

// 改成其他格式，比如：
// "1h 23m 45s"
// "1小时23分"
// 等等
```

---

## ✅ 测试检查清单

- [ ] 游戏开始时，时间从00:00开始
- [ ] 时间每秒增加
- [ ] 年龄每86400秒增加1天
- [ ] 喂食不再需要金币
- [ ] 清洁不再需要金币
- [ ] 游戏结束显示存活天数和总时长
- [ ] 重新开始后，存活天数重置，总时长继续累计
- [ ] UI正确显示时间和年龄

---

## 🎉 优势

### 简化游戏逻辑：
- ✅ 不需要管理金币经济
- ✅ 不需要设计赚钱机制
- ✅ 玩家可以自由照顾宠物
- ✅ 专注于时间管理而非资源管理

### 更好的游戏体验：
- ✅ 新手友好（不会因为没钱而卡住）
- ✅ 鼓励长时间游玩（追踪总时长）
- ✅ 明确的成就感（存活天数）
- ✅ 符合虚拟宠物养成游戏设计

---

**系统已完全从金币经济改为时间追踪！** 🎉

