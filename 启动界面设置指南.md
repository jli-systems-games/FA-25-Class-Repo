# 启动界面设置指南

## 🎯 功能说明

游戏启动流程：
1. **启动界面1** → 按 **↓** 键 → 
2. **启动界面2** → 按 **↓** 键 → 
3. **进入游戏**

## 📋 在Unity中设置

### 第1步：创建两个启动界面

在Canvas下创建两个面板：

```
Canvas
├── StartPanel1 ← 第一个启动界面
├── StartPanel2 ← 第二个启动界面
└── GameUI ← 你的游戏主界面（所有游戏UI的父对象）
```

#### A. 创建StartPanel1

1. **在Canvas下右键** → **UI → Panel**
2. 命名为 `StartPanel1`
3. 设置：
   - **Anchor**: 填充整个屏幕
   - **Background**: 你想要的背景图片或颜色
4. 在面板上添加内容：
   - 游戏标题
   - Logo
   - 提示文字："Press ↓ to continue"
5. **保持显示状态**（勾选GameObject）

#### B. 创建StartPanel2

1. 同样创建一个Panel，命名为 `StartPanel2`
2. 设置填充整个屏幕
3. 添加内容：
   - 游戏说明
   - 操作指南
   - 提示文字："Press ↓ to start game"
4. **初始隐藏**（取消勾选GameObject）

#### C. 设置GameUI

1. 在Hierarchy中创建空的GameObject，命名为 `GameUI`
2. 把你的所有游戏UI拖到这个对象下作为子对象：
   - MenuArea（菜单区域）
   - CatArea（小猫区域）
   - TopBar（信息栏）
   - 等等...
3. **初始隐藏**（取消勾选GameObject）

### 第2步：创建StartupManager

1. **在Hierarchy中找到GameInitializer**（如果没有就创建一个）
2. 添加 `StartupManager` 脚本到场景中的任意GameObject
   - 或者创建新的空GameObject命名为"StartupManager"并添加脚本

### 第3步：连接引用

选中StartupManager，在Inspector中连接：

```
启动界面：
├── Start Panel 1 ← 拖入StartPanel1
├── Start Panel 2 ← 拖入StartPanel2
└── Game UI ← 拖入GameUI（包含所有游戏UI的父对象）

提示文本（可选）：
├── Start Panel 1 Hint ← StartPanel1中的提示文本
└── Start Panel 2 Hint ← StartPanel2中的提示文本

设置：
├── Panel 1 Hint Text: "Press ↓ to continue"
├── Panel 2 Hint Text: "Press ↓ to start game"
└── Disable Game Managers On Start: ✓（勾选）
```

## 🎨 推荐的界面布局

### StartPanel1 示例（标题界面）

```
StartPanel1
├── Background (Image) ← 背景图
├── Title (TextMeshPro) ← "TAMAGOTCHI CAT"
│   └── Font Size: 72, 居中
├── Logo (Image) ← 游戏Logo（可选）
└── HintText (TextMeshPro) ← "Press ↓ to continue"
    └── Font Size: 24, 底部居中
```

### StartPanel2 示例（说明界面）

```
StartPanel2
├── Background (Image) ← 背景图
├── Title (TextMeshPro) ← "HOW TO PLAY"
├── Instructions (TextMeshPro) ← 操作说明
│   ├── "↑↓ - Select menu"
│   ├── "←→ - Select option"
│   └── "SPACE - Confirm"
└── HintText (TextMeshPro) ← "Press ↓ to start game"
    └── Font Size: 24, 底部居中
```

### GameUI 结构

```
GameUI (Empty GameObject) ← 所有游戏UI的父对象
├── MenuArea
├── CatArea
├── TopBar
├── SubMenuPanel
├── GameOverPanel
├── FeedPanel
├── PlayPanel
└── CleanPanel
```

## ⚙️ 设置说明

### Disable Game Managers On Start（推荐勾选）

- **勾选**: 启动界面显示时，游戏管理器（CatManager、UIManager、InputManager）会被禁用
  - 小猫不会衰减
  - 不会响应游戏输入（只响应↓键切换界面）
  - 进入游戏后才启用

- **不勾选**: 游戏管理器始终运行
  - 启动界面显示时小猫就开始衰减
  - 可能会响应游戏输入

**建议勾选！**

## 🎮 测试流程

1. **运行游戏**
2. **应该看到StartPanel1**（其他界面都隐藏）
3. **按 ↓ 键**
4. **切换到StartPanel2**（StartPanel1隐藏）
5. **再按 ↓ 键**
6. **进入游戏**（StartPanel2隐藏，GameUI显示，游戏开始）

## 💡 设计建议

### StartPanel1 设计（标题界面）

**风格：** 简洁、吸引眼球
- 大大的游戏标题
- 可爱的小猫像素图
- 闪烁的"Press ↓"提示
- 背景：纯色或简单图案

**示例内容：**
```
╔══════════════════════════════╗
║                              ║
║     TAMAGOTCHI  CAT         ║
║         🐱                   ║
║                              ║
║                              ║
║    Press ↓ to continue       ║
╚══════════════════════════════╝
```

### StartPanel2 设计（说明界面）

**风格：** 清晰、信息丰富
- 操作说明
- 游戏规则
- 小贴士

**示例内容：**
```
╔══════════════════════════════╗
║      HOW TO PLAY             ║
║                              ║
║  ↑↓ - Select menu           ║
║  ←→ - Select option         ║
║  SPACE - Confirm             ║
║  ESC - Pause                 ║
║                              ║
║  Keep your cat happy!        ║
║  Feed, Play, Clean           ║
║                              ║
║    Press ↓ to start          ║
╚══════════════════════════════╝
```

## 🎨 美化技巧

### 1. 添加渐入动画

在StartPanel上添加CanvasGroup组件，用脚本控制Alpha：
```csharp
// 渐入效果
CanvasGroup.alpha = 0 → 1 (1秒)
```

### 2. 提示文字闪烁

使用Animation或脚本让"Press ↓"文字闪烁：
```csharp
// 透明度循环
Alpha: 1 → 0.3 → 1 (重复)
```

### 3. 背景动画

- 移动的星星
- 缓慢滚动的背景
- 粒子效果

### 4. 音效

- 切换界面时播放音效
- 背景音乐在StartPanel1开始播放

## ❓ 常见问题

**Q: 按↓键没反应？**
A: 检查：
1. StartupManager是否已添加到场景
2. 是否连接了StartPanel1、StartPanel2、GameUI
3. Console是否有错误

**Q: 游戏UI一开始就显示了？**
A: 确保GameUI的GameObject初始是隐藏状态（取消勾选）

**Q: 进入游戏后游戏不工作？**
A: 确保"Disable Game Managers On Start"已勾选，让系统自动管理

**Q: 想跳过启动界面直接进游戏（测试用）？**
A: 在StartupManager上取消勾选StartPanel1和StartPanel2，或者直接让GameUI初始显示

**Q: 能改成其他键吗？**
A: 可以！修改StartupManager.cs中的Input.GetKeyDown(KeyCode.DownArrow)

## 🔄 流程图

```
游戏启动
    ↓
StartupManager初始化
    ↓
显示StartPanel1
禁用游戏管理器
    ↓
等待按↓键...
    ↓
显示StartPanel2
    ↓
等待按↓键...
    ↓
隐藏启动界面
显示GameUI
启用游戏管理器
    ↓
游戏开始！
```

## ✨ 完成！

设置完成后，你的游戏就有了专业的启动流程！

**提示：** 别忘了为两个启动界面设计漂亮的像素风格画面！🎨

---

**下一步：** 制作启动界面的美术资源和动画效果

