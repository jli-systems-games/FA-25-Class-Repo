using UnityEngine;

public class TicketManager : MonoBehaviour
{
    private GameObject currentPassenger;

    void Start()
    {
        currentPassenger = GetComponent<GameObject>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
